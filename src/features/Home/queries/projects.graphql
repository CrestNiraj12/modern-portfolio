query GetPinnedProjects {
  user(login: "CrestNiraj12") {
    pinnedItems(first: 6, types: REPOSITORY) {
      nodes {
        ... on Repository {
          id 
          name
          description
          url
        }
      }
    }
  }
}
