query GetPinnedProjects {
  user(login: "CrestNiraj12") {
    pinnedItems(first: 4, types: REPOSITORY) {
      nodes {
        ... on Repository {
          id 
          name
          description
          url
        }
      }
    }
  }
}
